let firstName: string = "Mark";

// all string methods are allowed on string types

let fullName: String = firstName + " " + "Doe";

fullName.split(seperator: " ");

// Math methods won't be accepted on String
// fullName = 123456;
// Math.log(fullName);
